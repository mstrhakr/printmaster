# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test

# Backup files
*.backup
*.bak

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.txt
coverage.html

# Go workspace file
go.work

# Build artifacts
agent/printmaster-agent
agent/printmaster-agent.exe
server/printmaster-server
server/printmaster-server.exe

# Build metadata (local build tracking)
.buildnumber
.lastversion
*.buildnumber
*.lastversion

# IDE and editor files
# Keep .vscode/ tasks and launch configs for team consistency
.vscode/*
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
logs/
agent/logs/
server/logs/

# Database files (runtime data, not schema)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Configuration files with secrets (keep examples)
config.ini
config.json
config.toml
agent/config.ini
agent/config.json
agent/config.toml
server/config.ini
server/config.toml
# Keep the examples:
!config.ini.example
!agent/config.ini.example
!*.example.toml

# OS-specific
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.Trashes

# Temporary files
tmp/
temp/
*.tmp

# Debug/profiling artifacts
debug/

# Build output
dist/
build/
!build/windows/
!build/windows/msi/
!build/windows/msi/**
bin/

# Windows service resources (generated)
agent/rsrc_windows_*.syso

# MIB walk files (optional - these can be large)
agent/walks/
*.walk

# Archive files
*.har

# Python (for tools/scripts)
__pycache__/
*.py[cod]
*$py.class
.Python
env/
venv/
ENV/
.venv

# Testing
testdata/
*_test_output/

# Coverage
*.coverprofile

# Comments extraction (workspace-specific)
all_comments.txt
comments_legacy.txt
agent/comments_list.txt

# Troubleshooting data review files
data_review/
logs_review/

# Build number tracking
.buildnumber
# Build artifacts
*.syso
versioninfo.json
coverage
coverage/
*.pem
certs/

agent/test_db/*
server/test_db/*

# Node / JS
node_modules/
npm-debug.log*
package-lock.json
.pnpm-debug.log

# Playwright browsers (if downloaded locally)
.playwright/

# npm coverage / reports
coverage/

# Optional local npm artifact
.npm/
test-results/
# Local env file for server (do NOT commit)\nserver/.env
server/.env
# Test artifacts (local)
server/test_results.json
test_run_full.txt
agent/docs/ICE Packet Capture.json
